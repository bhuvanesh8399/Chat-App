server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eazybyts_chat?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true

app:
  jwt:
    secret: CHANGE_ME_MIN_32_CHARS________________________________
